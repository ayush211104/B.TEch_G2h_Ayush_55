#include <stdio.h>
int main (){
    int  n, i ,key, found=0;
    printf("ENTER THE NUMBER OF ELEMENT");
    scanf("%d", &n);
    int arr[n];
    printf(" ENTER %d elements\n ", n);
    for(i=0; i<n;i++){
        scanf("%d", &arr[i]);
    }
    printf("ENTER ELEMENT TO SEARCH");
    scanf("%d", &ke)
}